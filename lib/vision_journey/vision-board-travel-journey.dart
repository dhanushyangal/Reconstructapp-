import 'package:flutter/material.dart';
import 'vision_board_journey.dart';

class VisionBoardTravelJourneyPage extends StatelessWidget {
  const VisionBoardTravelJourneyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const VisionBoardJourney();
  }
}
